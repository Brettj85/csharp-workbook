using System;

namespace gameboy
{
    class DisplayVertical
    {
        ConstructConsoleLine buildLine = new ConstructConsoleLine();
        char type = new char(*);
        buildLine.ReturnLine("formatMe",  true, 24, type);

    }
}
